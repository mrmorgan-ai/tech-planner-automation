name: ci

# Manual only while the repository is still being built out. To turn automatic
# runs back on, restore the push and pull_request triggers below.
on:
  workflow_dispatch:
  # push:
  #   branches: ['**']
  # pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm test
      - run: npm run build
