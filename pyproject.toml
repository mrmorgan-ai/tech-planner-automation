[project]
name = "tech-planner"
version = "0.1.0"
description = "Local Agile planning tool that drives an embedded Claude Code session"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
